//incomplete
#include<bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin>>t;
    while(t--)
    {
        char string[50];
        gets(string);
        for(int i=0;ch[i]!='\o';i++)
        {
            if(isdigit(ch[i]))
            {
                for(int j=i+1;ch[i]!='\o';j++)

            }
        }
    }
}